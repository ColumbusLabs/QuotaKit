# QuotaKit Mac 0.32.4.7 Release Checklist

Date: 2026-06-18

## Scope

- Payload-only Mac update for Keychain prompt suppression after app updates.
- No CloudKit model or sync schema changes.
- No iOS build-number or App Store Connect changes.

## CloudKit Schema Gate

- `./Scripts/verify-cloudkit-schema.sh` was run before release prep.
- Result: blocked because no CloudKit management token is saved locally.
- Release action: use `QUOTAKIT_SKIP_CLOUDKIT_SCHEMA_VERIFY=1` for this
  emergency Mac-only payload release.
- Risk assessment: accepted because this change does not alter CloudKit record
  types, fields, indexes, or sync serialization.

## Verification

- `swift test --filter KeychainMigrationTests`
- `swift test --filter KeychainPromptSafetyAuditTests`
- `swift test --filter LocalizationLanguageCatalogTests`
- `./Scripts/lint.sh lint`
- `make test`
