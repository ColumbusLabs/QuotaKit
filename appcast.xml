<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>CodexBar</title>
        <item>
            <title>0.23.4</title>
            <pubDate>Tue, 28 Apr 2026 20:37:32 -0700</pubDate>
            <link>https://raw.githubusercontent.com/o1xhack/CodexBar/mobile-dev/appcast.xml</link>
            <sparkle:version>58.4.1.3.1</sparkle:version>
            <sparkle:shortVersionString>0.23.4</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h2>CodexBar 0.23.4-Mobile 1.3.1</h2>
Hotfix that closes a long-standing Codex JSONL parser bug — pre-existing
all the way back to when the Codex scanner was first written, only
became visible recently because Codex CLI 0.125 changed its
<code>turn_context</code> shape. Caused 90%+ of Codex token usage to be silently
misattributed to <code>gpt-5</code>, no matter what model the user actually ran.
Every previous version's user (0.18 / 0.19 / 0.20 / 0.20.x / 0.21 /
0.22 / 0.23 / 0.23.1) is automatically corrected on first launch of
0.23.4 — the fingerprint mechanism rolls and triggers a fresh full
re-scan with the fixed parser.
<h3>Root cause</h3>
<code>Sources/CodexBarCore/Vendored/CostUsage/CostUsageScanner.swift:669</code>
declared <code>prefixBytes = 32 * 1024</code> for the Codex JSONL parser. Any line
larger than that gets <code>wasTruncated = true</code> from <code>CostUsageJsonl.scan</code>
and is skipped entirely.
Codex CLI 0.125+ ships <code>turn_context</code> events that bundle the project's
<code>AGENTS.md</code> / <code>CLAUDE.md</code> / <code>developer_instructions</code> into
<code>payload.user_instructions</code>, growing the line to <strong>~38–41 KB</strong> on a
typical project. Every <code>turn_context</code> was therefore truncated → skipped
→ <code>currentModel</code> never updated. All subsequent <code>event_msg/token_count</code>
events fell through the priority chain to <code>?? "gpt-5"</code> (line 763) and
got bucketed under <code>gpt-5</code> regardless of the real model.
The bug was masked because almost all earlier test fixtures included
<code>info.model</code> directly inside the token_count event (which bypasses
<code>currentModel</code>). Real Codex CLI 0.125 traffic doesn't.
<h3>Fix</h3>
<ul>
<li>Bumped <code>prefixBytes</code> from 32 KB to <code>maxLineBytes</code> (256 KB), matching</li>
</ul>
  what <code>CostUsageScanner+Claude.swift:80</code> and <code>PiSessionCostScanner.swift:280</code>
  already use. The cap remains in place for runaway-JSONL safety, just
  at a level that fits modern Codex events.
<ul>
<li>Bumped <code>CostUsagePricing.parserLogicVersion</code> from <code>1</code> → <code>2</code>. The</li>
</ul>
  <code>pricingFingerprint</code> mechanism (added in 0.23.1) detects the rolled
  fingerprint on first launch and runs a fresh full scan with the fixed
  parser. Caches written without a fingerprint at all (every release
  before 0.23.1) also fail the equality check and get invalidated, so
  long-time users on older versions are corrected too.
<ul>
<li>Two new regression tests in <code>CostUsageScannerTests.swift</code> pin the</li>
</ul>
  contract: one writes a single 50 KB turn_context + bare token_count
  (no <code>info.model</code>) and asserts attribution lands on <code>gpt-5.5</code>; the
  other simulates a mid-session model switch (gpt-5.4 → gpt-5.5) with
  two large turn_contexts and asserts the delta-split attribution
  lands on the right model in both segments. Both tests assert that
  <code>gpt-5</code> (the default fallback bucket) stays empty.
<h3>Code-review follow-ups (folded in before ship)</h3>
A self-review with codex-reviewer caught three P1 issues across the
0.23 / 0.23.1 / 0.23.4 commit cluster. All three are fixed in this
release rather than deferred:
<ul>
<li><strong>P1-1 — Lint guard fail-closed.</strong> The new <code>audit_parser_version</code></li>
</ul>
  check silently <code>return 0</code>'d when its base ref (<code>origin/mobile-dev</code>)
  was missing. CI shallow-clone checkouts (<code>actions/checkout</code> without
  <code>fetch-depth: 0</code>) would never have this ref, so parser changes could
  ship with no <code>parserLogicVersion</code> bump and the audit would still
  report success. Now it tries to fetch the missing ref first; if
  fetch fails it errors out instead of skipping. Explicit opt-out via
  <code>ALLOW_MISSING_BASE=1</code> for offline / fresh-fork-clone scenarios.
<ul>
<li><strong>P1-2 — Fingerprint must include prices, not just keys.</strong> The</li>
</ul>
  <code>pricingFingerprint</code> introduced in 0.23.1 hashed only model <strong>names</strong>,
  not their prices. A same-name reprice (existing model gets a new
  rate) wouldn't roll the fingerprint, leaving stale baked
  <code>costNanos</code> in <code>PiSessionCostCache</code> (and similarly in Claude
  caches that persist computed cost). Fingerprint now embeds the
  full price tuple per model — input / output / cacheRead /
  cacheCreation / threshold and above-threshold rates — so any
  edit invalidates every cache. New `pricingFingerprint rolls when
  a price changes` test pins the contract.
<ul>
<li><strong>P1-3 — iOS legacy-email normalization byte-matches Mac.</strong> Mac</li>
</ul>
  <code>AccountIdentityComputer</code> normalizes (NFC + percent-encode + length
  cap) before writing identifiers like <code>codex:email:...</code>. iOS's
  legacy-fallback synthesis in <code>CloudSyncReader.effectiveIdentifiers</code>
  used only <code>trim + lowercased</code>, so non-ASCII emails (e.g.
  <code>café@example.com</code>) split into separate cards across versions
  (Mac on 0.23+ writes <code>caf%C3%A9@…</code>; iOS synthesized
  <code>café@…</code> from a 0.20.x snapshot). Extracted the normalization
  to <code>Shared/iCloud/AccountIdentityNormalize.swift</code> so both sides use
  it. Pinned by paired tests on Mac and iOS asserting byte-identical
  output for the same fixture inputs.
<h3>Hardening — preventing the next prefixBytes-class bug</h3>
Two infrastructure additions so this kind of regression can't reach
users again:
<ul>
<li><strong>Lint guard.</strong> New <code>Scripts/lint.sh audit-parser-version</code> step</li>
</ul>
  fails CI when any of <code>CostUsageScanner.swift</code>,
  <code>CostUsageScanner+Claude.swift</code>, or <code>CostUsageJsonl.swift</code> change
  without a matching bump to <code>CostUsagePricing.parserLogicVersion</code>.
  Wired into the default <code>lint</code> command so <code>./Scripts/lint.sh lint</code>
  catches it pre-push and CI re-runs the same check on every PR.
  Cosmetic / comment-only edits can opt out via <code>ALLOW_PARSER_CHANGE=1</code>.
  Why: this 0.23.4 fix needed a manual <code>parserLogicVersion</code> bump for
  the cache to actually re-roll on user machines — easy to forget on
  future parser tweaks.
<ul>
<li><strong>Real-shape regression fixtures in tests.</strong> The new tests</li>
</ul>
  deliberately model real Codex CLI 0.125 output (multi-KB
  <code>user_instructions</code> payloads, no <code>info.model</code> on token_count)
  rather than the cooperative shape earlier tests used. Future
  scanner changes that re-introduce the truncation class of bugs
  break these tests immediately.
<h3>Notes</h3>
<ul>
<li>CFBundleVersion = <code>58.4.1.3.1</code>. Sparkle on 0.23 prompts the upgrade</li>
</ul>
  on next check-for-updates.
<ul>
<li>iOS unchanged (1.5.0 Build 96 / 98). Mac re-scan repushes corrected</li>
</ul>
  numbers to CloudKit; iOS reads automatically.
<ul>
<li>0.23.1 GitHub draft superseded — 0.23.4 carries the same cache</li>
</ul>
  invalidation infrastructure plus this parser fix, so 0.23.1 was
  never finalized.
<p><a href="https://github.com/o1xhack/CodexBar/blob/main/CHANGELOG.md">View full changelog</a></p>
]]></description>
            <enclosure url="https://github.com/o1xhack/CodexBar/releases/download/v0.23.4-mobile.1.3.1/CodexBar-0.23.4-mobile.1.3.1.zip" length="31592356" type="application/octet-stream" sparkle:edSignature="WL64h47/d+crXiTbuFqc+CUPE2y/KoMnupsC5tWVfFqK8D4Z/b764vMoO+0PNt68AzgML5vAheZEPkK+fd5gCw=="/>
        </item>
        <item>
            <title>0.23</title>
            <pubDate>Mon, 27 Apr 2026 18:03:38 -0700</pubDate>
            <link>https://raw.githubusercontent.com/o1xhack/CodexBar/mobile-dev/appcast.xml</link>
            <sparkle:version>56.1.3.1</sparkle:version>
            <sparkle:shortVersionString>0.23</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h2>CodexBar 0.23-Mobile 1.3.1</h2>
Mac-side rollup of upstream v0.21 / 0.22 / 0.23 (109 commits, 2 new providers, multiple provider enhancements) plus iOS 1.5.0 data-channel scaffolding pre-loaded so future iOS iterations don't need a new Mac release. Mobile companion stays at <strong>1.3.1</strong> — this is a Mac-only release; iOS users on 1.3.1 / 1.3.0 / 1.2.0 see existing 25 providers unchanged plus 2 new providers (Abacus AI, Mistral) as fallback cards.
<h3>Highlights — upstream 0.21–0.23 (Mac)</h3>
<ul>
<li><strong>Mistral provider</strong> (#607) — monthly spend tracking, browser-cookie import, manual cookies, CLI / token-account support. Thanks @welcoMattic!</li>
<li><strong>Abacus AI provider</strong> — ChatLLM and RouteLLM monthly compute-credit tracking with browser-cookie import, manual-cookie support, and monthly pace rendering. Thanks @ChrisGVE!</li>
<li><strong>Claude Designs / Daily Routines / Web Sonnet bars</strong> (#740) — live OAuth/Web quota data shown as additional usage bars on the Claude provider. Thanks @AISupplyGuy!</li>
<li><strong>Cursor Extra usage</strong> menu metric for on-demand budgets (#789). Thanks @huiye98!</li>
<li><strong>Synthetic</strong> — parses live 5-hour / weekly / search quota payloads with continuous reset/regeneration details (#732). Thanks @baanish!</li>
<li><strong>Codex Pro $100 plan</strong> support across OAuth / OpenAI web / menu / CLI; <strong>GPT-5.5 / GPT-5.5 Pro pricing</strong> for the local cost scanner.</li>
<li><strong>Codex</strong> — opt-in OpenAI web extras for fresh installs with battery-saver toggle; restored OpenAI web dashboard fetching on the new analytics route; Edge browser-cookie import.</li>
<li><strong>Antigravity</strong> — restored localhost endpoint/token probing across newer builds with async TLS challenge handling, retry on API-level errors.</li>
<li><strong>z.ai</strong> — preserve weekly + 5-hour token quotas together, surface 5-hour lane correctly across menu/menu bar.</li>
<li><strong>OpenCode</strong> — weekly pace visualization (reserve / expected / "Lasts until reset" details like Codex/Claude).</li>
<li><strong>Menu shortcuts</strong> ⌘R / ⌘, / ⌘Q while status menu is open (#737); fix macOS 26 RenderBox icon regression (#677); merged-menu width/alignment fixes.</li>
<li><strong>Battery / refresh</strong> — cut menu redraw churn, skip background work for unavailable providers, reuse cached OpenAI web views (#708).</li>
<li><strong>Confetti</strong> — opt-in celebration when weekly limits reset after active use (#785).</li>
</ul>
<h3>Mobile 1.3.1 — Mac-side data channel pre-loaded for iOS 1.5.0 (Option B)</h3>
Mac 0.23 now writes 6 new optional <code>Shared/</code> Codable types to CloudKit so iOS 1.5.0 can render Abacus / Mistral structured detail, Synthetic 3-lane, Claude extras, Cursor Extra without ever needing a Mac patch later. iOS 1.3.x silently drops these unknown fields via existing <code>decodeIfPresent</code> (Build 79 forward-compat regression test pins this behavior) — bit-for-bit safe.
Types added: <code>SyncAbacusCreditSummary</code> · <code>SyncMistralUsageSummary</code> · <code>SyncSyntheticQuotaSummary</code> · <code>SyncClaudeExtraBars</code> · <code>SyncCursorExtraUsage</code> plus 6 optional fields on <code>ProviderUsageSnapshot</code>. <code>SyncCoordinator</code> adds 6 mapping sites mirroring how <code>SyncPerplexityCreditSummary</code> was added in 0.20.3.
<h3>L1 ghost-records cleanup (root-cause fix)</h3>
Closes the bug class user reported on iOS 1.3.0 right after the 0.20.3 release (duplicate Codex cards from upgrade-induced identity drift, plus stale Perplexity card after disable). iOS 1.3.1 Build 94 shipped a display-time filter; this Mac release adds the *root cause* fix in <code>SyncCoordinator</code>:
<ul>
<li><strong>Provider-disable hook</strong> — when a previously-enabled provider transitions to disabled, delete its CKRecord from <code>DeviceProvidersZone</code> instead of leaving it as a stale ghost.</li>
<li><strong>Account-identity-drift cleanup</strong> — when a provider's composite key changes (e.g., Codex OAuth refactor between Mac versions), find and delete the stale recordName for that provider before writing the new one.</li>
</ul>
Together, the 6 known orphan-producing state transitions (provider enable/disable × 3 identity-rewrite paths) now self-heal.
<h3>Notes</h3>
<ul>
<li>CFBundleVersion = <code>56.1.3.1</code>. <code>BUILD_NUMBER</code> jumped 55.3 → <strong>56</strong> for the upstream-aligned slot. Sparkle <code>MOBILE_VERSION</code> tracks the current iOS train (1.3.1 — App Store hotfix).</li>
<li>Multi-device / multi-version compat verified against TestFlight Build 95 and Macs on legacy + per-provider zones.</li>
</ul>
<hr/>
<p><a href="https://github.com/o1xhack/CodexBar/blob/main/CHANGELOG.md">View full changelog</a></p>
]]></description>
            <enclosure url="https://github.com/o1xhack/CodexBar/releases/download/v0.23-mobile.1.3.1/CodexBar-0.23-mobile.1.3.1.zip" length="31271884" type="application/octet-stream" sparkle:edSignature="poy/XS0OPHUUpTRSHtW3gdsuTSBP0MPmCRyiMFRGQIdOzwt7WpRDb0p6NF/qk/+Fl20EPomApCi6V9O5Ob32Dw=="/>
        </item>
        <item>
            <title>0.20.3</title>
            <pubDate>Fri, 24 Apr 2026 13:42:02 -0700</pubDate>
            <link>https://raw.githubusercontent.com/o1xhack/CodexBar/mobile-dev/appcast.xml</link>
            <sparkle:version>55.3.1.2.0</sparkle:version>
            <sparkle:shortVersionString>0.20.3</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h2>CodexBar 0.20.3-Mobile 1.3.0</h2>
Mobile 1.3.0 release. Mac 0.20.3 (and the preceding 0.20.2) are small user-invisible data-layer patches on top of 0.20.0 that enable the iOS 1.3.0 experience — everything user-facing in this release lives on iPhone.
<h3>Highlights — Mobile 1.3.0</h3>
<ul>
<li><strong>2 new providers on iPhone</strong> — Perplexity (3-segment credit detail + Pro/Max badge + renewal countdown) and OpenCode Go, with dedicated colors and Mac→iPhone push notifications.</li>
<li><strong>Codex multi-account cards</strong> — when you run multiple Codex accounts / workspaces, each card shows its email / workspace subtitle.</li>
<li><strong>Faster, leaner sync</strong> — per-provider CloudKit records with zlib compression (typical sync transfer drops from ~2 MB to a few dozen KB); silent-push-driven refresh updates views without pull-to-refresh.</li>
<li><strong>Cold-start polish</strong> — Usage tab no longer flashes blank on launch; transient CloudKit failures preserve cached data instead of blanking the screen.</li>
<li><strong>Per-device Mac version in About & Sync</strong> — see which Mac is running which CodexBar version, with an orange "Update available" chip on any Mac that's not on the latest.</li>
<li><strong>Multi-device + multi-version correctness</strong> — Subscription Utilization numbers stay consistent between the aggregate view and each provider's detail; older Macs can no longer silently drop fields the newer Mac wrote.</li>
</ul>
<h3>Mobile 1.3.0 — new providers</h3>
<ul>
<li><strong>Perplexity detail page</strong> — 3-segment credit bar (recurring / bonus / purchased), Pro/Max plan badge, renewal and promo-expiration countdowns, dollar balance.</li>
<li><strong>Perplexity + OpenCode Go push notifications</strong> — both now in the 25-provider × 2-state push set (50 zones), with the provider name baked into the alert body in all 4 languages.</li>
<li><strong>Unified provider color palette</strong> — consolidated across 5 previously-drifted call sites; OpenCode Go no longer collides with OpenCode Zen.</li>
<li><strong>Codex cards</strong> with ≥2 accounts show email / workspace subtitles; single-account setups stay minimal.</li>
</ul>
<h3>Mobile 1.3.0 — sync & stability</h3>
<ul>
<li><strong>Per-provider CloudKit records</strong> in a new <code>DeviceProvidersZone</code>, zlib-compressed. Older iPhones fall back to the legacy monolithic zone with zero regression.</li>
<li><strong>Silent-push-driven refresh</strong> — iPhone wakes silently when Mac writes, applies the delta, views refresh in the background.</li>
<li><strong>SwiftData cold-start hydrate</strong> — Cost tab no longer flashes a stale value before settling; Usage tab cold-start blank (two root causes — date-strategy mismatch + ghost records) fixed.</li>
<li><strong>Transient-failure defense</strong> — if CloudKit is momentarily unreachable, cached data is preserved instead of the screen blanking.</li>
<li><strong>Multi-device merge correctness</strong> — aggregate and per-provider utilization views share the same daily-peak semantic; cross-version field preservation stops older Macs from dropping fields the newer Mac knows about.</li>
<li><strong>Forward-compatible wire format</strong> — iPhones on today's build silently tolerate unknown fields from future Mac versions.</li>
</ul>
<h3>Mobile 1.3.0 — polish</h3>
<ul>
<li><strong>Per-device Mac version</strong> in About & Sync, with a "· Update available" chip on older Macs.</li>
<li>4-language localization for all new strings (en / zh-Hans / zh-Hant / ja).</li>
<li>Comprehensive regression-guard test fixtures for realistic multi-device, cross-version data distributions.</li>
</ul>
<h3>Mac — 0.20.0 → 0.20.3</h3>
Two user-invisible Mac-side data-layer patches that power the iOS 1.3.0 experience above — per-provider CloudKit records with zlib compression (0.20.2) and Perplexity credit-pool pass-through (0.20.3). Mac user-facing behavior is unchanged since 0.20.0.
Mac 0.20.0 (2026-04-17) was the fork's alignment with upstream CodexBar 0.20. Highlights:
<ul>
<li><strong>Codex system account switching</strong> — switch between system accounts / profiles without manually logging out and back in (contribution by @ratulsarna).</li>
<li><strong>Perplexity provider</strong> (PR #606) — recurring, bonus, and purchased-credit tracking; Pro/Max plan detection; browser-cookie auto-import with manual-cookie fallback.</li>
<li><strong>OpenCode Go</strong> — separate provider from OpenCode Zen, with 5-hour / weekly / monthly web usage tracking, widget integration, and browser-cookie support.</li>
<li><strong>Claude token/cost accuracy</strong> — fixes cross-file double counting of subagent JSONL logs and streaming chunk deduplication; adds <code>claude-sonnet-4-6</code> pricing.</li>
</ul>
<hr/>
2026-04-23 Mobile 1.3.0 发布。Mac 0.20.3（以及前一个 0.20.2）只是在 0.20.0 之上的两个 Mac 端用户不可见数据层补丁，用来让 iOS 1.3.0 的能力落地 —— 本次所有用户可见变化都在 iPhone 端。
<h3>亮点 — Mobile 1.3.0</h3>
<ul>
<li><strong>iPhone 新增 2 个 Provider</strong> —— Perplexity（三段式 credit 详情页 + Pro/Max 徽章 + 续费倒计时）和 OpenCode Go，带专属配色以及 Mac→iPhone 推送通知。</li>
<li><strong>Codex 多账号卡片</strong> —— 同时运行多个 Codex 账号 / workspace 时，每张卡片在副标题显示对应的 email / workspace。</li>
<li><strong>更快更省的同步</strong> —— 按 provider 拆分的 CloudKit 记录 + zlib 压缩（典型同步流量从约 2 MB 降到几十 KB）；静默推送驱动刷新，视图不用下拉即可更新。</li>
<li><strong>冷启动抛光</strong> —— Usage tab 冷启动不再白屏；CloudKit 临时失败时保留缓存而不是清空界面。</li>
<li><strong>About & Sync 按设备显示 Mac 版本</strong> —— 清晰看到每台 Mac 运行的 CodexBar 版本，落后版本带橙色"可升级"标识。</li>
<li><strong>跨 Mac + 跨版本数据正确性</strong> —— 订阅利用率在聚合视图与单 provider 详情之间数字一致；旧 Mac 不再静默丢弃新 Mac 写入的字段。</li>
</ul>
<h3>Mobile 1.3.0 — 新 Provider</h3>
<ul>
<li><strong>Perplexity 详情页</strong> —— 三段式 credit 柱（recurring / 赠送 / 购买）、Pro/Max 套餐徽章、续费 / 赠送到期倒计时、美元余额。</li>
<li><strong>Perplexity + OpenCode Go 推送通知</strong> —— 两者均加入 25 Provider × 2 状态的推送集合（50 个 zone），Provider 名称烤进 alertBody，4 语言本地化。</li>
<li><strong>统一 Provider 配色</strong> —— 收敛之前在 5 个调用点漂移的实现；OpenCode Go 视觉上不再与 OpenCode Zen 混淆。</li>
<li><strong>Codex 卡片</strong> 在 ≥2 账号时副标题显示 email / workspace；单账号保持极简。</li>
</ul>
<h3>Mobile 1.3.0 — 同步 & 稳定性</h3>
<ul>
<li><strong>按 provider 拆分的 CloudKit 记录</strong>，新 zone <code>DeviceProvidersZone</code>，zlib 压缩。老版本 iPhone 回落到传统整体 zone，零回退。</li>
<li><strong>静默推送驱动刷新</strong> —— Mac 写入时 iPhone 静默唤醒，应用 delta，视图在后台刷新。</li>
<li><strong>SwiftData 冷启动水合</strong> —— Cost tab 不再闪一下旧值；Usage tab 冷启动白屏（两个根因 —— 日期策略不一致 + 幽灵记录）已修复。</li>
<li><strong>瞬时失败防御</strong> —— CloudKit 临时不可达时保留缓存，而不是清空屏幕。</li>
<li><strong>多设备合并正确性</strong> —— 聚合视图与单 Provider 利用率视图共用"日峰值"语义；跨版本字段保留，防止旧 Mac 静默丢弃新 Mac 知道的字段。</li>
<li><strong>前向兼容的 wire 格式</strong> —— 今日构建的 iPhone 静默容忍未来 Mac 版本加入的未知字段。</li>
</ul>
<h3>Mobile 1.3.0 — 打磨</h3>
<ul>
<li>About & Sync 按设备显示 Mac 版本，落后 Mac 带"· 可升级"标识。</li>
<li>所有新字符串 4 语言本地化（en / zh-Hans / zh-Hant / ja）。</li>
<li>针对真实多设备、跨版本数据分布的回归测试 fixture 全面扩展。</li>
</ul>
<h3>Mac — 0.20.0 → 0.20.3</h3>
配合 iOS 1.3.0 落地的两个 Mac 端用户不可见数据层补丁 —— 按 provider 拆分的 CloudKit 记录 + zlib 压缩（0.20.2），以及 Perplexity credit 分段字段透传（0.20.3）。Mac 端用户可见行为自 0.20.0 以来无变化。
Mac 0.20.0（2026-04-17）是本 fork 对齐上游 CodexBar 0.20 的主版本，亮点：
<ul>
<li><strong>Codex 系统账号切换</strong> —— 不用手动登出再登入即可切换系统账号 / profile（@ratulsarna 贡献）。</li>
<li><strong>Perplexity 服务商</strong>（PR #606）—— recurring / 赠送 / 购买三段式 credit 追踪，Pro/Max 套餐识别，浏览器 cookie 自动导入加手动 cookie 兜底。</li>
<li><strong>OpenCode Go</strong> —— 从 OpenCode Zen 分离出独立 provider，支持 5 小时 / 周 / 月 web 用量追踪、widget、浏览器 cookie。</li>
<li><strong>Claude token/费用修正</strong> —— 修复子 agent JSONL 跨文件重复计数和流式分片去重；新增 <code>claude-sonnet-4-6</code> 定价。</li>
</ul>
<p><a href="https://github.com/o1xhack/CodexBar/blob/main/CHANGELOG.md">View full changelog</a></p>
]]></description>
            <enclosure url="https://github.com/o1xhack/CodexBar/releases/download/v0.20.3-mobile.1.3.0/CodexBar-0.20.3-mobile.1.3.0.zip" length="29459926" type="application/octet-stream" sparkle:edSignature="3um1b+r7i2UHxb5ePE0R+au39gIOvNCsFf0y0FfrxaJAdh6eKdYi4piIAZw/cPDHt+JWybcUR+tt721mIZiYCw=="/>
        </item>
        <item>
            <title>0.14.0</title>
            <pubDate>Thu, 25 Dec 2025 03:56:15 +0100</pubDate>
            <link>https://raw.githubusercontent.com/steipete/CodexBar/main/appcast.xml</link>
            <sparkle:version>41</sparkle:version>
            <sparkle:shortVersionString>0.14.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h2>CodexBar 0.14.0</h2>
<ul>
<li>Antigravity: new local provider for the Antigravity language server (Claude + Gemini quotas) with an experimental toggle; improved plan display + debug output; clearer not-running/port errors; hide account switch.</li>
<li>Status: poll Google Workspace incidents for Gemini + Antigravity; Status Page opens the Workspace status page.</li>
            <li>Settings: add Providers tab; move cost usage + status toggles to General; keep display controls in Advanced.</li>
<li>Menu/UI: widen the menu for four providers; cards/charts adapt to menu width; tighten provider switcher/toggle spacing; keep menus refreshed while open.</li>
<li>Gemini: hide the dashboard action when unsupported.</li>
<li>Claude: fix Extra usage spend/limit units (cents); improve CLI probe stability; surface web session info in Debug.</li>
<li>OpenAI web: fix dashboard ghost overlay on desktop (WebKit keepalive window).</li>
<li>Debug: add a debug-lldb build mode for troubleshooting.</li>
</ul>
<p><a href="https://github.com/steipete/CodexBar/blob/main/CHANGELOG.md">View full changelog</a></p>
]]></description>
            <enclosure url="https://github.com/steipete/CodexBar/releases/download/v0.14.0/CodexBar-0.14.0.zip" length="5382347" type="application/octet-stream" sparkle:edSignature="qhj8jnqH/eOXEP2plbZyTb8w1x0Afcpix0kC+pzMh5jX9Lkf3Yg2Bvd6/vsKdhOObEwJjQZRb/RKiSBx55J3DA=="/>
        </item>
        <item>
            <title>0.13.0</title>
            <pubDate>Wed, 24 Dec 2025 01:53:35 +0100</pubDate>
            <link>https://raw.githubusercontent.com/steipete/CodexBar/main/appcast.xml</link>
            <sparkle:version>40</sparkle:version>
            <sparkle:shortVersionString>0.13.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/steipete/CodexBar/releases/download/v0.13.0/CodexBar-0.13.0.zip" length="5099083" type="application/octet-stream" sparkle:edSignature="EmhEmcTFQEpuQG6vZ288l9j278VjnpQdQcmZnU1JDvzERVTtnIg+8RDlsHV9niwejaOBPX6mvXST3vNJHpwADA=="/>
        </item>
        <item>
            <title>0.12.0</title>
            <pubDate>Tue, 23 Dec 2025 04:39:05 +0100</pubDate>
            <link>https://raw.githubusercontent.com/steipete/CodexBar/main/appcast.xml</link>
            <sparkle:version>39</sparkle:version>
            <sparkle:shortVersionString>0.12.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <enclosure url="https://github.com/steipete/CodexBar/releases/download/v0.12.0/CodexBar-0.12.0.zip" length="4888386" type="application/octet-stream" sparkle:edSignature="tQMHO/RNAbvwRHXYnLAkNV2ksiV722qR8fEYzcbipgetacfPnwnLJ0Pe/lAiZ03PBmj3BkisHb74GosUlSV+DQ=="/>
        </item>
    </channel>
</rss>